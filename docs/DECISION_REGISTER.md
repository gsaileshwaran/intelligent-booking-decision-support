# PVK CINEMAS — DECISION REGISTER
**Document Identifier:** PVK-DEC-REG-001  
**Status:** SPECIFICATION BASELINE DECISION REGISTER  
**Version:** 2.0 (Phase 0.6 Frozen Baseline)  
**Authority:** Derived strictly from the 10 Authoritative PVK Cinemas Project Documents (PVK-SRS-001, PVK-ARCH-001, PVK-API-001, PVK-UIUX-001, PVK Cinemas Final Database Design, PVK-IMPL-001, PVK-TEST-001, PVK-RTM-001, PVK-DEP-001, PVK-SDP-001)  
**Rule:** Only formally approved decisions are marked as `APPROVED — IMPLEMENTATION BASELINE`. All unapproved decisions remain explicitly recorded as `OPEN / UNRESOLVED`.

---

## 1. MASTER DECISION REGISTER

| ID | Question / Unresolved Item | Source Document(s) | Affected System Areas | Approved Resolution / Documented Options | Impact on Implementation | Blocking? | Decision Status |
|---|---|---|---|---|---|---|---|
| **DECISION-001** | What is the exact authentication token/session mechanism and logout invalidation semantics? | SRS §19 (TBD-001), Arch §30 (ARCH-TBD-001), API Spec §5, §6, §30 (API-TBD-001) | Security architecture, `/api/v1/auth/login`, `/api/v1/auth/logout`, `Authorization: Bearer <token>`, frontend session store | **APPROVED RESOLUTION:** Stateless JWT with server-side token revocation support. Requests transmit `Authorization: Bearer <token>`. JWTs are short-lived (30m). Logout invalidates token on server via revocation store. | Logical security contract frozen; physical store selection (Redis vs DB table vs in-memory) deferred to Phase P4. | **RESOLVED FOR P0** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-002** | What is the exact numeric response-time target for search and show discovery under load? | SRS §10 (NFR-001), SRS §19 (TBD-002), Arch §18, §30 (ARCH-TBD-003), API Spec §25 | Performance test plan, search pipeline, API SLAs | Options not specified by authoritative documents (SRS intentionally leaves numeric targets as TBD). Recommended baseline: p95 < 500ms discovery, p95 < 800ms hybrid search. | Prevents defining exact pass/fail latency gates for NFR-001 during Phase P14 performance testing. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-003** | What is the production hosting model, availability SLA target, and disaster recovery objectives (RTO/RPO)? | SRS §10 (NFR-004), SRS §19 (TBD-003), Arch §30 (ARCH-TBD-004), Deployment Plan §4, §13, §28 | Production infrastructure, high availability clustering, backup frequency, failover procedures | Options not specified by authoritative documents (managed cloud containers vs VMs vs Kubernetes left open as ARCH-TBD-004). | Affects operational sizing and cloud infrastructure provisioning in Phase P14. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-004** | What are the exact customer-facing filter criteria and sorting options for movie discovery? | SRS §19 (TBD-004), UI Spec §30 | `/api/v1/movies`, Movie Listing (UI-02), Search Results (UI-04) | Baseline filters (status, city, genre, language, format) are documented in API Spec §9.1. Advanced sorting facets remain open. | Affects query parameter schema and UI filtering components in Phase P8/P11. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-005** | What are the exact movie catalogue management permissions for Theatre Managers? | SRS §6.3, §19 (TBD-005), DB Design §7, API Spec §7, §16, §30 (API-TBD-004) | `/api/v1/manager/movies`, Theatre Manager UI (UI-11, UI-14), Authorization matrix | **APPROVED RESOLUTION:** Theatre Managers have **READ-ONLY** access to the global movie catalogue (`GET /manager/movies`). They may view movies to schedule shows. All `POST/PATCH` mutation operations on `/manager/movies` are formally removed; movie management belongs strictly to Super Admin (`/admin/movies`, UI-19). | Enforces single source of truth for catalogue metadata; eliminates multi-theatre catalogue conflicts. | **RESOLVED FOR P0** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-006** | What is the search evaluation dataset, golden query set, and target quality metrics? | SRS §12 (AI-011), SRS §19 (TBD-006), Arch §30 (ARCH-TBD-003), Deployment Plan §14 | Search test suite, AI retrieval regression, Release Gate G7 | Options not specified by authoritative documents. Recommended: 50 golden queries covering titles, genres, actors, languages, formats. Target: NDCG@10 >= 0.85. | Blocks formal quality verification of AI/search ranking in Phase P9 / Gate G7. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-007** | How is customer seat reservation, holding, and payment handled in PVK Cinemas? | SRS §2, §6, Arch §15, API Spec §20 | `SHOW_SEAT`, Seat Availability UI (UI-08), Booking & Payment flows | **SUPERSEDED & EXPANDED — APPROVED RESOLUTION:** Interactive seat selection, 5-minute temporary seat holds with live countdown timer, simulated checkout with dummy payment methods (Dummy Card, Dummy UPI, Test Wallet), deterministic outcomes (SUCCESS/FAILED), official booking confirmation/e-ticket view, and customer booking history are **IN SCOPE**. Commercial third-party ticketing aggregators and real financial payment gateways remain **STRICTLY OUT OF SCOPE**. | Upgrades schema with 4 normalized 3NF entities (`SEAT_HOLD`, `BOOKING`, `BOOKING_SEAT`, `PAYMENT`) and introduces pessimistic locking concurrency protection. | **RESOLVED** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-008** | What is the exact retention policy and pruning schedule for `SEARCH_QUERY` and `AUDIT_LOG`? | SRS §19 (TBD-008), Arch §30 (ARCH-TBD-006), Deployment Plan §13, §28 | Database maintenance jobs, disk space management, compliance | Options not specified by authoritative documents. Recommended: 90 days for `SEARCH_QUERY`, 365 days for `AUDIT_LOG`. | Affects scheduled archival jobs and database storage growth planning in Phase P14. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-009** | What is the exact search engine and vector-store technology selection? | Arch §11, §30 (ARCH-TBD-002), API Spec §30 (API-TBD-005), Deployment Plan §28 | AI/Search service deployment unit, vector embedding storage, indexing pipeline | Options mentioned in documents: OpenSearch, Elasticsearch, PostgreSQL pgvector, FAISS, or embedded vector library. Left open as ARCH-TBD-002. | Determines technology dependencies and architecture for Phase P9 (Search Subsystem). | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-010** | What is the API versioning policy beyond version 1.0? | Arch §30 (ARCH-TBD-005), API Spec §30 (API-TBD-002) | API routing, URL path structure, backward compatibility | (1) URI path versioning (`/api/v1`, `/api/v2`); (2) Header-based versioning. | Version 1.0 is locked to `/api/v1`; future policy does not block v1 development. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-011** | What are the exact operational rate-limiting thresholds per IP or account? | API Spec §28, §30 (API-TBD-003) | API Gateway / Reverse Proxy, Spring Security rate limiter, HTTP 429 responses | Options not specified by authoritative documents. Recommended: 120 req/min/IP for public endpoints, 60 req/min for search. | Operational security against DoS; does not block core functional development. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-012** | What is the exact audit event catalogue and trigger list for mutations writing to `AUDIT_LOG`? | SRS §14, API Spec §30 (API-TBD-006), Arch §9 | Backend JPA entity listeners / AOP audit logging interceptors | Options not specified by authoritative documents. Core administrative and security mutations identified in DECISION_RESOLUTION_PLAN. | Dictates which specific mutation operations synchronously insert rows into `AUDIT_LOG` in Phase P3/P13. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-013** | What is the final visual brand system (typography, color tokens, spacing scale)? | UI Spec §30 (UI-TBD-001) | Frontend CSS design tokens, typography, component styles | Options not specified by authoritative documents. Recommended: Modern cinema dark palette (Obsidian, Gold, Crimson, Inter/Outfit). | Aesthetic and UI consistency; needed before Phase P10 (Frontend Foundation). | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-014** | What is the final authentication and session restoration UX? | UI Spec §30 (UI-TBD-002) | Login form (UI-09), session restore, token refresh, redirect on 401 | Dependent on DECISION-001. Frontend inspects token validity via `GET /auth/me` on load; redirects to login on 401. | Determines frontend auth store and HTTP client interceptor behavior in Phase P10. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-015** | What are the exact home-page merchandising and featured-content ranking rules? | UI Spec §30 (UI-TBD-004) | Home screen (UI-01) carousel, featured movie order | Options not specified by authoritative documents. Recommended: order by release date descending, filtered by selected city. | Presentation-layer ordering in Phase P11; does not impact backend schema. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-016** | What is the exact product analytics and telemetry event scope? | UI Spec §30 (UI-TBD-005) | Frontend analytics tracking, telemetry endpoints | Options not specified by authoritative documents. Client-side telemetry is optional; search telemetry tracked in `SEARCH_QUERY`. | Telemetry is optional in v1 baseline. | NO (for Phase 1) | OPEN / UNRESOLVED |
| **DECISION-017** | Which database migration engine shall be used (Flyway vs Liquibase)? | Implementation Plan §7, Deployment Plan §9 | Phase P1 database migrations, CI pipeline | **APPROVED RESOLUTION:** **Flyway** with native SQL migrations (`V1__...sql`) located in `src/main/resources/db/migration`. DDL scripts derive 100% directly from `PVK_Cinemas_Final_Database_Design.docx`. | Establishes deterministic, versioned, checksum-protected migration execution for Phase P1. | **RESOLVED FOR P0** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-018** | How do public customers self-register an account? | SRS §6.1 (`FR-001`–`FR-005`), §6.2 (`FR-010`–`FR-012`), API Spec §7 | Customer registration, `/api/v1/auth/register`, Screen UI-09 | **APPROVED RESOLUTION:** Add public customer registration endpoint: `POST /api/v1/auth/register`. Transactionally creates `USER` (`account_status = 'ACTIVE'`), `CUSTOMER_PROFILE`, and assigns `ROLE_CUSTOMER`. Validates name, email, phone, and password hash. Formally registered in `CHANGE-API-001`. | Enables public customer onboarding, customer preferences, and authenticated search history (`FR-093`). Zero schema changes. | **RESOLVED FOR P0** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-019** | How are `SHOW_SEAT` records initialized when a show is created? | SRS §6.9 (`FR-080`), Arch §15, API Spec §11.1, §20, DB Design §3 | `POST /manager/theatres/{id}/shows`, `SHOW_SEAT` availability | Architecture §15 and API Spec §20 establish that show creation is a single transaction covering show-seat rows for the target screen. Synchronously inserts `SHOW_SEAT` rows for all active screen seats in `AVAILABLE` status. | Guarantees complete seat availability on show creation; eliminates partial show states. | NO (Resolved in Docs) | OPEN / ARCHITECTURALLY RESOLVED (P1) |
| **DECISION-020** | How are Super Admin and Theatre Manager roles separated? | SRS §5, Arch §8, Security Baseline | User roles, RBAC, Multi-Portal Access Control | **APPROVED RESOLUTION:** Super Admin (`admin@pvkcinemas.com`, Arun Sharma) has platform-wide authority only (`ROLE_SUPER_ADMIN`) and cannot simultaneously hold `ROLE_THEATRE_MANAGER`. Theatre Manager (`manager@pvkcinemas.com`, Ravi Kumar) has strictly theatre-scoped operational authority (`ROLE_THEATRE_MANAGER`) for assigned theatre `PVK Cineplex Chennai`. Access to other theatres or admin operations yields HTTP 403 Forbidden. | Prevents role conflation and enforces zero-trust object-level authorization. | **RESOLVED** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-021** | How is concurrent seat holding protected against race conditions? | SRS §6.9, Arch §15, DB Design §10 | `SHOW_SEAT`, `/api/v1/shows/{showId}/seats/hold` | **APPROVED RESOLUTION:** Pessimistic row-level locking (`SELECT ... FOR UPDATE` via `PESSIMISTIC_WRITE`) on `SHOW_SEAT` rows within an atomic `@Transactional` boundary. Concurrent requests for the same seat receive HTTP 409 Conflict. Seat holds expire automatically after 300 seconds (5 minutes). | Guarantees mutual exclusion and prevents duplicate seat reservations across concurrent users. | **RESOLVED** | **APPROVED — IMPLEMENTATION BASELINE** |
| **DECISION-022** | How is City-Scoped show and theatre filtering enforced? | SRS §6.3, API Spec §9, Frontend Views | `CITY -> THEATRE -> SCREEN -> SHOW -> SHOW_SEAT` | **APPROVED RESOLUTION:** City filtering is strictly enforced via database relational joins (`theatre.city_id = :cityId`) across all movie and showtime endpoints. Selecting a city (e.g. Chennai) returns exclusively theatres and showtimes physically located in that city, with zero cross-city leakage. | Guarantees relational integrity and prevents out-of-city multiplex leakage. | **RESOLVED** | **APPROVED — IMPLEMENTATION BASELINE** |

---

## 2. APPROVAL SUMMARY

- **Total Baseline Decisions Formally Approved:** **9** (`DECISION-001`, `DECISION-005`, `DECISION-007`, `DECISION-017`, `DECISION-018`, `DECISION-019`, `DECISION-020`, `DECISION-021`, `DECISION-022`)
- **Total Remaining P0 Decisions:** **0** (All P0 items formally resolved and tested)
- **Specification Status:** **FULLY APPROVED AND ALIGNED WITH IMPLEMENTATION**
